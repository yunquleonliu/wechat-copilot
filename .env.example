# Required
WORK_DIR=/datapai/Tough-C-Profiler.worktrees/copilot-worktree-2026-03-24T02-26-05

# Local Claude Code CLI (auto-detected from VSCode extension)
# CLAUDE_BIN=/home/vtuser/.vscode-server/extensions/github.copilot-chat-0.40.1/dist/cli.js

# Sidecar endpoints (defaults shown)
# OMNICODE_URL=http://localhost:8081/v1
# OMNICODE_MODEL=omnicode-9b
# GEMMA_URL=http://localhost:8080/v1
# GEMMA_MODEL=gemma2-9b

# WeChat: comma-separated iLink user IDs allowed to use the bot
# Get your ID from: npm run setup (printed after QR login)
# Leave empty to allow all (unsafe on shared machines)
WECHAT_ALLOWED_FROM=

# Agent query timeout in ms (default 180000 = 3 min)
# QUERY_TIMEOUT_MS=180000
