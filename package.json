{
  "name": "wechat-copilot",
  "version": "0.1.0",
  "description": "WeChat → local Copilot CLI bridge with OmniCode/Gemma sidecars",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx src/index.ts",
    "setup": "tsx src/setup.ts",
    "build": "tsc",
    "start": "node --import tsx src/index.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/qrcode-terminal": "^0.12.2",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
